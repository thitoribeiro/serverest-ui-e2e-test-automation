/**
 * @file listUsersElements.js
 * Mapeamento de elementos da tela de Lista de Usuários.
 * Clean Code: nomes claros, imutabilidade, fonte única de verdade.
 */

const SELECTORS = Object.freeze({
  heading: 'h1'
});

module.exports = SELECTORS;
